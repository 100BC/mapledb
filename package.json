{
  "name": "mooseical",
  "version": "0.0.0",
  "description": "Mooseical Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "config/*",
    "libs/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "generate": "turbo run generate",
    "clean": "turbo run clean && rm -rf node_modules .turbo .pnpm-debug.log",
    "clean:install": "pnpm clean && pnpm install",
    "lint": "turbo run lint",
    "format": "prettier --write '**/*.{ts,tsx,md}'"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@6.31.0"
}

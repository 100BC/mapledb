{
  "name": "mapledb",
  "version": "0.0.0",
  "description": "MapleDB Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "config/*",
    "libs/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm kill-ports && turbo run dev",
    "start": "pnpm kill-ports && turbo run start --parallel",
    "postinstall": "pnpm generate",
    "generate": "turbo run generate",
    "codegen": "turbo run codegen",
    "clean": "turbo run clean && rm -rf node_modules .turbo .pnpm-debug.log",
    "clean:install": "turbo run clean && pnpm install && turbo run generate",
    "kill-ports": "kill-port 3000 3001 3002",
    "lint": "turbo run lint",
    "format": "prettier --write '**/*.{ts,tsx,md}'",
    "db:deploy": "turbo run db:deploy"
  },
  "devDependencies": {
    "kill-port": "^1.6.1",
    "prettier": "^2.6.2",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@6.31.0"
}

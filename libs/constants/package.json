{
  "name": "@mapledb/constants",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit --incremental false && eslint --fix '**/*.ts'",
    "clean": "rm -rf node_modules .turbo .pnpm-debug.log dist"
  },
  "devDependencies": {
    "@mapledb/eslint-config": "workspace:0.0.0",
    "@mapledb/tsconfig": "workspace:0.0.0",
    "eslint": "^8.23.0",
    "typescript": "^4.8.2"
  }
}

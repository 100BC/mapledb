query GetMusician($id: String!) {
  musician(id: $id) {
    name
    city {
      name
      province
    }
    appleLink
    bandcampLink
    soundcloudLink
    spotifyLink
    youtubeLink
    isGroup
    disbanded
  }
}

query getMusicianName($id: String!) {
  musician(id: $id) {
    name
  }
}
